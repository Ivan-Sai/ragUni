# Pinned for reproducibility. Versions below are what the test suite
# passes against; upgrade intentionally with `pip install -U <pkg>` and
# document the bump in the commit message.

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Database
motor==3.7.1
pymongo==4.16.0
certifi==2025.11.12
pyopenssl==25.3.0

# LangChain — RAG Orchestration (1.x ecosystem)
langchain==1.2.10
langchain-core==1.2.17
langchain-community==0.4.1
langchain-mongodb==0.11.0
langchain-openai==1.1.10
langchain-text-splitters==1.1.1

# Vector Search & Embeddings
fastembed==0.7.4

# LLM Integration
openai==2.26.0

# Document Processing
PyPDF2==3.0.1
python-docx==1.1.2
pandas==3.0.1
openpyxl==3.1.5
python-magic-bin==0.4.14

# Text Processing
tiktoken==0.12.0

# Rate Limiting
slowapi==0.1.9

# Utilities
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.0.1

# Auth
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
email-validator==2.3.0

# Observability (optional at runtime — gracefully skipped when SENTRY_DSN
# and ENABLE_METRICS are absent, but required for the production profile)
sentry-sdk[fastapi]==2.18.0
prometheus-fastapi-instrumentator==7.0.2

# Testing
pytest==9.0.2
pytest-asyncio==1.3.0
httpx==0.28.1
pytest-cov==7.0.0
